import type {
  ImageProps,
  TextProps,
  TouchableOpacityProps,
} from 'react-native';

export type CardRootProps = TouchableOpacityProps;
export type CardImageProps = ImageProps;
export type CardTextProps = TextProps;
